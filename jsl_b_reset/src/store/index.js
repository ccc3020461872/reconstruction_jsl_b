import { createStore } from 'vuex'
import user from './user'
import app from './app'
export default createStore({
  state: () => ({
  }),
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    user,
    app,
  }
})
